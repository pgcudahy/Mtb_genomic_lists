* To make this git friendly, follow the steps
** Install nbstripout
`pip install --upgrade nbstripout`
** Set up the git filter using .gitattributes
`nbstripout --install --attributes .gitattributes`
